# 🎨 Frontend-Backend Connection Flow

## 📊 Architecture Overview

```
┌─────────────────────────────────────────────────────────────┐
│                     MOBILE/WEB APP                           │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Your Frontend Application                            │  │
│  │  (React Native / Flutter / Web)                       │  │
│  └──────────────────────────────────────────────────────┘  │
│                           │                                  │
│                           │ HTTP Requests                    │
│                           │                                  │
│                           ▼                                  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  API Configuration                                    │  │
│  │  BASE_URL: http://185.193.19.244:8080/api           │  │
│  │  TIMEOUT: 30000ms                                     │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                           │
                           │ Internet
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│              CONTABO SERVER (185.193.19.244)                 │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Docker Container: yoraa-api-prod                     │  │
│  │  Port: 8080                                           │  │
│  │  Environment: Production                              │  │
│  │                                                        │  │
│  │  ┌────────────────────────────────────────────────┐  │  │
│  │  │  Node.js Backend API                           │  │  │
│  │  │  - Authentication                               │  │  │
│  │  │  - Products & Categories                        │  │  │
│  │  │  - Cart & Orders                                │  │  │
│  │  │  - Payment (Razorpay)                           │  │  │
│  │  │  - Shiprocket Integration                       │  │  │
│  │  └────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────┘  │
│                           │                                  │
│                           ▼                                  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  MongoDB Database (Remote)                            │  │
│  │  - User Data                                          │  │
│  │  - Products & Inventory                               │  │
│  │  │  - Orders & Transactions                           │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔄 Request Flow Example

### 1️⃣ User Login Flow

```
┌─────────────┐
│  User App   │
└──────┬──────┘
       │
       │ 1. POST /api/auth/login
       │    { phone: "9876543210", password: "****" }
       │
       ▼
┌─────────────────────────────────────────┐
│  http://185.193.19.244:8080/api         │
│                                          │
│  Backend processes:                     │
│  - Validates credentials                │
│  - Generates JWT token                  │
│  - Returns user data                    │
└─────────────────────────────────────────┘
       │
       │ 2. Response
       │    { success: true, token: "jwt...", user: {...} }
       │
       ▼
┌─────────────┐
│  User App   │
│  Stores     │
│  Token      │
└─────────────┘
```

### 2️⃣ Authenticated Request Flow

```
┌─────────────┐
│  User App   │
└──────┬──────┘
       │
       │ 1. GET /api/products
       │    Headers: { Authorization: "Bearer jwt..." }
       │
       ▼
┌─────────────────────────────────────────┐
│  http://185.193.19.244:8080/api         │
│                                          │
│  Backend processes:                     │
│  - Verifies JWT token                   │
│  - Fetches products from DB             │
│  - Returns product list                 │
└─────────────────────────────────────────┘
       │
       │ 2. Response
       │    { success: true, products: [...] }
       │
       ▼
┌─────────────┐
│  User App   │
│  Displays   │
│  Products   │
└─────────────┘
```

### 3️⃣ Order Creation Flow

```
┌─────────────┐
│  User App   │
└──────┬──────┘
       │
       │ 1. POST /api/orders/create
       │    Headers: { Authorization: "Bearer jwt..." }
       │    Body: { items: [...], address: {...}, payment: "razorpay" }
       │
       ▼
┌─────────────────────────────────────────┐
│  http://185.193.19.244:8080/api         │
│                                          │
│  Backend processes:                     │
│  - Validates user & items               │
│  - Creates Razorpay order               │
│  - Saves order to DB                    │
│  - Returns order details                │
└─────────────────────────────────────────┘
       │
       │ 2. Response
       │    { success: true, orderId: "...", razorpayOrderId: "..." }
       │
       ▼
┌─────────────┐
│  User App   │
│  Shows      │
│  Payment UI │
└─────────────┘
```

---

## 🌐 Network Configuration

### Development vs Production

```
┌──────────────────────────┐        ┌──────────────────────────┐
│     DEVELOPMENT          │        │      PRODUCTION          │
├──────────────────────────┤        ├──────────────────────────┤
│ localhost:8080           │   →    │ 185.193.19.244:8080     │
│ http://localhost:8080    │        │ http://185.193.19.244   │
│                          │        │                          │
│ - Local testing          │        │ - Real server            │
│ - Fast debugging         │        │ - Live users             │
│ - No internet required   │        │ - Requires internet      │
└──────────────────────────┘        └──────────────────────────┘
```

---

## 🔐 Authentication Token Flow

```
┌──────────────────────────────────────────────────────────────┐
│                    Token Lifecycle                            │
└──────────────────────────────────────────────────────────────┘

1. LOGIN
   ┌─────────┐  phone/password  ┌─────────┐
   │ User    │ ───────────────→  │ Backend │
   └─────────┘                   └─────────┘
                                      │
                    ┌─────────────────┘
                    │ Generate JWT Token
                    ▼
   ┌─────────┐  token + user    ┌─────────┐
   │ User    │ ←───────────────  │ Backend │
   └─────────┘                   └─────────┘
       │
       │ Store token in:
       │ - AsyncStorage (React Native)
       │ - SecureStorage (Flutter)
       │ - LocalStorage (Web)
       ▼
   [Token Stored]

2. SUBSEQUENT REQUESTS
   ┌─────────┐  Request + Token ┌─────────┐
   │ User    │ ───────────────→  │ Backend │
   └─────────┘  Authorization:   └─────────┘
                Bearer token          │
                                      │ Verify token
                                      │
                    ┌─────────────────┘
                    │ Token valid?
                    ▼
                  YES ─→ Process request
                   │
                  NO ──→ Return 401
                         (Unauthorized)

3. TOKEN EXPIRY
   ┌─────────┐  Request + Token ┌─────────┐
   │ User    │ ───────────────→  │ Backend │
   └─────────┘                   └─────────┘
                                      │
                    ┌─────────────────┘
                    │ Token expired!
                    ▼
   ┌─────────┐  401 Error       ┌─────────┐
   │ User    │ ←───────────────  │ Backend │
   └─────────┘                   └─────────┘
       │
       │ Clear token
       │ Redirect to login
       ▼
   [Re-authenticate]
```

---

## 📱 Platform-Specific Setup

### iOS (React Native / Flutter)

```
┌────────────────────────────────────────┐
│         iOS App                        │
├────────────────────────────────────────┤
│                                        │
│  Info.plist Configuration:             │
│  ┌──────────────────────────────────┐ │
│  │ NSAppTransportSecurity           │ │
│  │   └─ NSExceptionDomains          │ │
│  │       └─ 185.193.19.244          │ │
│  │           └─ Allow HTTP           │ │
│  └──────────────────────────────────┘ │
│                                        │
│  This allows HTTP connections         │
│  to production server                 │
└────────────────────────────────────────┘
```

### Android (React Native / Flutter)

```
┌────────────────────────────────────────┐
│       Android App                      │
├────────────────────────────────────────┤
│                                        │
│  network_security_config.xml:          │
│  ┌──────────────────────────────────┐ │
│  │ domain-config                    │ │
│  │   cleartextTrafficPermitted=true │ │
│  │   └─ 185.193.19.244              │ │
│  └──────────────────────────────────┘ │
│                                        │
│  AndroidManifest.xml:                  │
│  ┌──────────────────────────────────┐ │
│  │ networkSecurityConfig=           │ │
│  │   @xml/network_security_config   │ │
│  └──────────────────────────────────┘ │
│                                        │
│  This allows HTTP connections         │
│  to production server                 │
└────────────────────────────────────────┘
```

---

## 🔍 API Endpoint Structure

```
http://185.193.19.244:8080/api
│
├─ /auth
│  ├─ POST   /login              - User login
│  ├─ POST   /register           - User registration
│  ├─ POST   /send-otp           - Send OTP
│  ├─ POST   /verify-otp         - Verify OTP
│  └─ POST   /logout             - User logout
│
├─ /products
│  ├─ GET    /                   - List all products
│  ├─ GET    /:id                - Get single product
│  ├─ GET    /category/:catId    - Products by category
│  └─ GET    /search?q=keyword   - Search products
│
├─ /categories
│  ├─ GET    /                   - List all categories
│  └─ GET    /:id                - Get single category
│
├─ /cart
│  ├─ GET    /                   - Get user cart
│  ├─ POST   /add                - Add item to cart
│  ├─ PUT    /update/:id         - Update cart item
│  └─ DELETE /remove/:id         - Remove from cart
│
├─ /orders
│  ├─ GET    /                   - Get user orders
│  ├─ GET    /:id                - Get order details
│  ├─ POST   /create             - Create new order
│  └─ GET    /track/:id          - Track order
│
├─ /profile
│  ├─ GET    /                   - Get user profile
│  ├─ PUT    /                   - Update profile
│  └─ POST   /address            - Add/update address
│
├─ /razorpay
│  ├─ POST   /create-order       - Create Razorpay order
│  └─ POST   /verify-payment     - Verify payment
│
└─ /health                        - Health check
```

---

## 🚀 Quick Start Checklist

```
┌────────────────────────────────────────────────────────┐
│  PRE-DEPLOYMENT CHECKLIST                              │
├────────────────────────────────────────────────────────┤
│                                                        │
│  [ ] Update API_BASE_URL to:                          │
│      http://185.193.19.244:8080/api                   │
│                                                        │
│  [ ] Test health endpoint:                            │
│      http://185.193.19.244:8080/health                │
│                                                        │
│  [ ] Configure iOS Info.plist (for HTTP)              │
│                                                        │
│  [ ] Configure Android network_security_config.xml    │
│                                                        │
│  [ ] Implement token storage & retrieval              │
│                                                        │
│  [ ] Add request interceptor for auth token           │
│                                                        │
│  [ ] Add response interceptor for 401 errors          │
│                                                        │
│  [ ] Test login flow                                  │
│                                                        │
│  [ ] Test authenticated requests                      │
│                                                        │
│  [ ] Test error handling                              │
│                                                        │
│  [ ] Build & test on physical devices                 │
│                                                        │
│  [ ] Ready for deployment! 🚀                         │
│                                                        │
└────────────────────────────────────────────────────────┘
```

---

## 📊 Response Format

All API responses follow this structure:

```javascript
// Success Response
{
  "success": true,
  "message": "Operation successful",
  "data": {
    // Response data here
  }
}

// Error Response
{
  "success": false,
  "message": "Error message",
  "error": {
    "code": "ERROR_CODE",
    "details": "Additional error details"
  }
}
```

---

## 🎯 Integration Steps

```
STEP 1: Configuration
└─→ Update API_BASE_URL
    └─→ Set timeout values
        └─→ Configure environment

STEP 2: Network Setup
└─→ iOS: Update Info.plist
    └─→ Android: Update network_security_config.xml
        └─→ Test network connectivity

STEP 3: API Service
└─→ Create API client (axios/dio)
    └─→ Add request interceptor (auth token)
        └─→ Add response interceptor (error handling)
            └─→ Test API calls

STEP 4: Testing
└─→ Test health endpoint
    └─→ Test authentication
        └─→ Test product listing
            └─→ Test cart operations
                └─→ Test order creation

STEP 5: Deploy
└─→ Build production app
    └─→ Test on devices
        └─→ Deploy to stores
            └─→ Monitor & maintain
```

---

**Ready to integrate?** Follow `FRONTEND_PRODUCTION_SETUP_CONTABO.md` for detailed instructions! 🚀
